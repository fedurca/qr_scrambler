(function (global) {
  "use strict";
  // Keep in sync with package.json (build embeds the same value into the title).
  global.APP_VERSION = "1.2.4";
})(typeof window !== "undefined" ? window : globalThis);
